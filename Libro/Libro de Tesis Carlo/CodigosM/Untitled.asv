map = [-2.5, 0;
       -7, -34.5;
       40, -43.5;
      % 124, -61;
      % 112, -113
      ];

center = [-7, -34.5];
figure(1)
plot(map(:,1),map(:,2),'ko-');
hold on;
grid on;
plot(center(:,1),center(:,2),'k*');
plot(CirculoExterno(:,3),CirculoExterno(:,4),'k');